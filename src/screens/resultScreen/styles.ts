import {StyleSheet} from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
  },
  title: {
    fontWeight: 'bold',
    margin: 16,
    fontSize: 24,
  },
  definition: {
    marginHorizontal: 16,
  },
});
